# Superseded figure versions

Not for use. `fig3-weight-sankey.SUPERSEDED.html` is the pre-fix version of the weight figure, replaced by
`../fig3-weight-cascade.html`. It asserts 35.65%, states "34 provinces dashboard-selectable" as fact, tags the
IBEI weights (a), and uses the bare word "services" — all of which the adopted conventions and the contested
formulations in `briefs/SOT-standing-rules.md` now forbid.

Retained only because `figures/` is untracked in git, so this is the sole pre-fix baseline against which a
regression check is possible. Delete once the figures are committed.
